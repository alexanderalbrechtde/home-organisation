<?php

require_once(__DIR__ . '/header.phtml');
?>
<main class="tasks-page">
    <div class="container">
        <section class="tasks-header">
            <h1>Alle laufenden Tasks</h1>
            <p class="muted">Hier siehst du alle offenen Aufgaben.</p>

        </section>

        <section class="card">
            <header class="card__header">
                <h2>
                    <span class="title-text">Laufende Tasks</span>
                </h2>
            </header>
            <div class="card__body">
                <?php
                if (!empty($items)): ?>
                    <ul class="list" id="task-list">
                        <?php
                        foreach ($items as $t): ?>
                            <li class="task-card <?= !empty($t['status']) ? 'is-' . htmlspecialchars(
                                    $t['status'],
                                    ENT_QUOTES,
                                    'UTF-8'
                                ) : '' ?>">
                                <div class="task-header">
                                    <h3>
                                        <?= $t['title'] ?>
                                        <?php
                                        if (!empty($t['roomTagHtml'])): ?>
                                            <?= $t['roomTagHtml'] ?>
                                        <?php
                                        endif; ?>
                                    </h3>
                                </div>
                                <div class="task-body">
                                    <p><strong>Endet in:</strong> <?= $t['dueText'] ?></p>
                                    <p><?= $t['notesHtml'] ?></p>
                                </div>
                            </li>
                        <?php
                        endforeach; ?>
                    </ul>
                <?php
                else: ?>
                    <p>Keine Tasks vorhanden.</p>
                <?php
                endif; ?>
            </div>
        </section>
    </div>
</main>


<?php
require_once(__DIR__ . '/footer.phtml');
?>
