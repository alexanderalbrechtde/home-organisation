<?php

require_once(__DIR__ . '/header.phtml');
?>
    <section class="konto-page">
        <header class="page-header">
            <h1>Mein Konto</h1>
            <p class="page-subtitle">Übersicht deiner Daten & Möglichkeit zur Aktualisierung.</p>
        </header>

        <?php
        $name = htmlspecialchars($user['name'] ?? '', ENT_QUOTES, 'UTF-8');
        $email = htmlspecialchars($user['email'] ?? '', ENT_QUOTES, 'UTF-8');
        ?>

        <div class="card">
            <h2 class="card-title">Aktuelle Daten</h2>
            <div class="form-grid">
                <div class="field">
                    <label>Name</label>
                    <input type="text" value="<?= $name ?>" readonly>
                </div>
                <div class="field">
                    <label>E‑Mail</label>
                    <input type="email" value="<?= $email ?>" readonly>
                </div>
                <div class="field">
                    <label>Passwort</label>
                    <input type="password" value="••••••••" readonly>
                    <small class="help">Aus Sicherheitsgründen wird dein Passwort nicht im Klartext angezeigt.</small>
                </div>
            </div>
        </div>

        <div class="card">
            <h2 class="card-title">Daten ändern</h2>
            <form class="form" action="/konto/update" method="post" autocomplete="off">
                <div class="form-grid">
                    <div class="field">
                        <label for="name">Neuer Name</label>
                        <input id="name" name="name" type="text" value="<?= $name ?>" placeholder="Name anpassen">
                    </div>
                    <div class="field">
                        <label for="email">Neue E‑Mail</label>
                        <input id="email" name="email" type="email" value="<?= $email ?>" placeholder="E‑Mail anpassen">
                    </div>
                    <div class="field">
                        <label for="password">Neues Passwort</label>
                        <input id="password" name="password" type="password" minlength="8"
                               placeholder="Neues Passwort (optional)">
                        <small class="help">Leer lassen, wenn du das Passwort nicht ändern willst.</small>
                    </div>
                    <div class="field">
                        <label for="password_confirm">Passwort bestätigen</label>
                        <input id="password_confirm" name="password_confirm" type="password" minlength="8"
                               placeholder="Wiederhole das neue Passwort">
                    </div>
                </div>
                <div class="form-actions">
                    <button class="btn btn-primary" type="submit">Speichern</button>
                    <a class="btn btn-ghost" href="/konto">Abbrechen</a>
                </div>
            </form>
        </div>
    </section>
<?php
require_once(__DIR__ . '/footer.phtml');
?>